<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bjsxt.mapper.AdminMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.bjsxt.pojo.Admin">
        <id column="aid" property="aid" />
        <result column="aname" property="aname" />
        <result column="apwd" property="apwd" />
        <result column="aphone" property="aphone" />
        <result column="starttime" property="starttime" />
    </resultMap>
    <!--Mybatis自定义方法-->
    <select id="listResult" parameterType="java.util.Map" resultMap="BaseResultMap">
        select *
        from t_admin
        <where>
            1 = 1
            <if test="aid != null and aid != ''">
                and aid = #{aid, jdbcType=VARCHAR}
            </if>
            <if test="aname != null and aname != ''">
                and aname = #{aname, jdbcType=VARCHAR}
            </if>
            <if test="starttime != null and starttime != ''">
                and starttime &gt;= #{starttime, jdbcType=VARCHAR}
            </if>
        </where>
        order by aid asc
    </select>
</mapper>
